# Main Firmware

This folder is reserved for the integrated CalmNest ESP32 S3 application firmware.

The validated OTA engineering reference currently lives in `../OTA/` and should be merged into the main application only after the production update strategy, security, partitioning, and recovery behavior are finalized.
